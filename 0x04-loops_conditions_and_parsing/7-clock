#!/usr/bin/env bash
# script displays time for 12 hrs and 59 min
#+ hours from 0 to 12 and minutes from 1 to 59

m=0
while [ "$m" -le 12 ]
do
	echo "Hour: $m"
	(( m++ ))

    	n=1
    	while [ "$n" -le 59 ]
    	do
		echo "$n"
		(( n++ ))
	done
done
